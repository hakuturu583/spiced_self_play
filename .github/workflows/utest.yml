name: Unit tests

on:
  pull_request:
    branches: [ main, 'emerge/**' ]
  push:
    branches: [ main, 'emerge/**' ]

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Free up disk space
        run: |
          echo "Before cleanup:"
          df -h
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
          sudo apt-get clean
          sudo rm -rf ~/.cache/pip /tmp/* /var/tmp/*
          echo "After cleanup:"
          df -h

      - name: Install dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y build-essential cmake

      - name: Upgrade pip
        run: python -m pip install -U pip

      - name: Install pufferlib
        run: |
          pip install -e . --no-cache-dir
        env:
          TMPDIR: ${{ runner.temp }}/build
          PIP_NO_CACHE_DIR: 1
          PUFFER_CPU: 1

      - name: Compile C extensions
        run: python setup.py build_ext --inplace --force
        env:
          PUFFER_CPU: 1

      - name: Run C ini parsing tests
        run: |
          chmod +x tests/ini_parser/build_n_test.sh
          ./tests/ini_parser/build_n_test.sh

      - name: Run python ini parsing tests
        run: python tests/test_drive_config.py

      - name: Run drive/ocean pytest suite
        run: |
          pip install pytest
          python -m pytest -v \
            tests/test_drive_map_types.py \
            tests/test_drive_per_agent_logging.py \
            tests/test_drive_scenario_length.py \
            tests/test_eval_manager.py \
            tests/test_validation_replay_html.py \
            pufferlib/ocean/benchmark/test_geometry.py \
            pufferlib/ocean/benchmark/test_map_metrics.py \
            pufferlib/ocean/benchmark/test_road_edges.py \
            pufferlib/ocean/benchmark/test_ttc.py

      - name: Run drive training smoke
        env:
          PUFFER_CPU: 1
        run: python -m pytest -v tests/test_drive_train.py
